enum Role {
    USER
    ADMIN
}

type User {
    id: ID!
    name: String!
    email: String!
    role: Role!
    reservations: [Reservation!]!
}

input UserInput {
    name: String!
    email: String!
    role: Role!
}
